# Copy to .env for local overrides (never commit secrets).

# PORT=4100
# HOST=127.0.0.1

# Force cloud mode (no disk music library) even in development:
# PRISMATIC_CLOUD=1

# Force local library APIs even when NODE_ENV=production (not for Railway):
# PRISMATIC_LOCAL=1
