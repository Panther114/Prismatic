# Desktop packaging only — not used on Railway / web deploy.
appId: app.prismatic.desktop
productName: Prismatic
copyright: Copyright © Prismatic
directories:
  output: release
  buildResources: build
icon: build/icon.png
files:
  - dist/**/*
  - dist-server/**/*
  - server/**/*
  - electron/**/*
  - build/icon.png
  - build/icon.ico
  - public/music-note.png
  - public/favicon.svg
  - package.json
  - node_modules/**/*
asar: true
asarUnpack:
  # Native optional bits only — server runs from asar as bundled ESM
  - node_modules/@napi-rs/**/*
  - node_modules/@esbuild/**/*
extraMetadata:
  main: electron/main.cjs
win:
  icon: build/icon.ico
  target:
    - target: nsis
      arch:
        - x64
nsis:
  oneClick: true
  perMachine: false
  allowToChangeInstallationDirectory: false
  createDesktopShortcut: true
  createStartMenuShortcut: true
  shortcutName: Prismatic
